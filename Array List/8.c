#include<stdio.h>

int main()
{
    int n;
    printf("Enter the number of elements: ");
    scanf("%d",&n);

    int a[n];
    int i,j,mindiff = 0, diff;

    for(i = 0 ; i < n ; i++)
    {
        for(j = i+1 ; j < n ; j++)
        {
            if(a[i] - a[i+1] > 0)
                
        }
    }
    return 0;
}
